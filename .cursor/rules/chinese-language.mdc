---
description: 本仓库回复与文档使用中文；Markdown 公式用 $ 定界
alwaysApply: true
---

# 中文沟通与文档规则

## 适用范围

本规则对本仓库内所有 Agent 会话**始终生效**。

## 必须使用中文

- 聊天中的解释、分析、总结、建议、错误说明
- 新建或更新的 Markdown 文档正文（含实验报告、交接说明、设计笔记）
- Commit 说明与 PR 描述（若用户要求提交时）
- 代码审查意见与测试失败分析

## 保持英文/原文的内容

- 变量名、函数名、类名、模块路径、Git 分支名
- 代码块、配置文件键名、CLI 标志
- 数学公式与标准符号
- 引用论文标题、第三方库名、UCI 数据集名

## 文档分工

- `Program.md`：项目交接，**仅人类编辑**；Agent 可读引用，无明确指令不得修改。
- `Prompt.md`：项目目标与规则，人类维护。
- 实验报告：Agent 可生成，**正文用中文**；指标列名可与 JSON 保持一致。

## Markdown 数学公式

本仓库所有 Markdown 文件中的公式**必须使用 `$` 定界**，禁止使用 `\(` `\)`、`\[` `\]`：

| 类型 | 写法 | 示例 |
|------|------|------|
| 行内公式 | `$...$` | `密度 $q_\theta(x)$ 满足…` |
| 行间公式 | `$$...$$` | 公式单独成段，前后各一行 `$$` |

聊天回复中的公式也遵循同一约定。

## 质量要求

- 使用完整、清晰的中文句子，避免电报式缩写。
- 技术术语首次出现时可附英文原名，例如：张量列车（Tensor Train, TT）。
- 代码引用仍使用仓库规定的 citation 格式（`startLine:endLine:filepath`）。

## 例外

用户当次消息明确要求使用其他语言时，以用户指令为准。
